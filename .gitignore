# Secrets and raw data (never commit)
.env
data/

# Personal / planning docs
# (PREREGISTRATION.md and its amendments are committed as of Amendment 1 — owner decision 2026-07-24)
leonardo.md
memory/
/*.pdf
app/consent_policy_draft.md

# Local session logs
results/*.log

# Regenerable Stage 1E training-split run artifacts (175M; summaries and
# the committed reports are the record)
results/adaptive_train_*/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# macOS
.DS_Store

# NOTE: uv.lock is intentionally NOT ignored — the lock file is committed.

# Regenerable overnight export artifacts (rebuild via overnight.py export)
results/overnight_exp245/prompts_static.jsonl
results/overnight_exp245/tasks_static.jsonl

# Stage 1E confirm run: regenerable exports (rebuild via confirm_run.py export;
# every file's sha256 is recorded in stage1e_confirm/export_manifest.json) and
# the raw per-arm completion records (GB-scale; summaries are committed).
results/stage1e_confirm/prompts_static_*.jsonl
results/stage1e_confirm/tasks_static.jsonl
results/stage1e_confirm/pack_local.json
results/stage1e_confirm/pack_node_s*.json
results/stage1e_confirm/arms/*/records.jsonl
results/stage1e_confirm/node/
